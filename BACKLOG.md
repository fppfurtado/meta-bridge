# Backlog

## Próximos

- Ajustar template/escopo de `/journal-close` pra síntese humano-amigável — sessão CC `generalizacao-mecanizacao` (no `meta-system`, 2026-06-11) produziu síntese manual de fechamento em formato divergente do `/journal-close` atual. Operador identificou explicitamente que template atual "parece feito mais pra máquinas/sistemas que pra humanos — sensação de abrir o git log". Divergência em 2 eixos: (i) **escopo** — síntese manual inclui DONE + TODO/WAITING + insights/reflexão direcional, vs DONE-only orientado a fechar trabalho mecânico; (ii) **template** — linguagem humana 2ª pessoa + bullets aninhados + narrativa fluida + GTD markers nativos, vs git-log-like flat (DONE task + commit hash + plan ref). Escopo do /triage a decidir: **(a)** refactor `/journal-close` existente (expandir escopo + reescrever template; risco: workflow runbook-style fica pesado); **(b)** skill nova `/session-close` ou similar pra cobertura ampla, `/journal-close` permanece mecânica DONE-only (risco: 2 skills competindo confunde invocação); **(c)** refactor `/journal-close` com flag/modo (`--reflective` ativa template expandido; default mecânico — compromisso entre a+b). Cascateamento possível com BACKLOG entry "Faceta 1 da revisitação retroativa ADR-016 (`meta-system`)" — Bloco 4 decidiu materializar este repo como CLI standalone (`mb` ou similar); se materializar, template pode mover pra módulo Python com Click/Typer + flag de output. Considerar também `feedback_qualidade_sobre_custo_arquitetural` refinada (custo migração isolado é argumento fraco quando há ganho de qualidade tangível). Referência editorial concreta: bucket `- #meta-system` em `~/Notes/logseq/journals/2026_06_11.md` mostra o template humano-amigável em ação. Contexto integral em `.claude/local/NOTES.md` § 2026-06-11. Cross-refs: ADR-016 + ADR-017 do meta-system; sessão CC `generalizacao-mecanizacao` upstream. Gatilho de execução: operador escolher, coordenado com Bloco 4 ou independente.

- Materializar Tier 1 candidato como CLI (substituindo MCP per critério target-aware) — decisão upstream em `meta-system/docs/plans/revisitacao-retroativa-materializacoes-pre-adr-016.md` Bloco 4 (commit `6586e5a` do meta-system). ADR-016 do meta-system § Critério aplicado: MCP-1/2/3 = 0/3 (ops filesystem atomic; sem state in-memory; sem mid-turn discovery essencial — operador invoca skills explicitamente); CLI-1/2/3/4 = 4/4. ADR-013 § Princípios herdados "MCP canonical para knowledge layer" refinado por ADR-016: princípio condicionado a assimetria substantiva (que não dispara aqui). Escopo do /triage: (a) materializar CLI standalone `meta_bridge.cli` (entry-point `mb` ou similar via Click/Typer) com 4 sub-comandos correspondendo às 4 skills atuais — `mb journal-note`, `mb journal-close`, `mb init-project`, `mb weekly-review`; (b) ADR-002 interno registrando materialização CLI substituindo Tier 1 MCP candidato implícito (cross-ref a ADR-016 cross-cutting + ADR-013 § Princípios herdados refinamento); (c) atualizar snapshot doutrinal `TIER_SNAPSHOT_2026_06_11` em `meta-portability-mcp/.../tools.py` removendo meta-bridge da lista Tier 1 MCP candidato (provavelmente coordenado com migração meta-portability-mcp → CLI per Bloco 1); (d) decisão de cascateamento: skills atuais (`/journal-note` etc.) permanecem como thin orchestrators invocando CLI internamente OU descontinuam (operador roda `mb` direto). Sem rename do repo. Contexto integral em `.claude/local/NOTES.md` § 2026-06-11 (entry sobre ajuste `/journal-close` é demanda relacionada). Cross-refs: ADR-016 cross-cutting do meta-system + ADR-013 § Princípios herdados refinado. Gatilho de execução: operador escolher.

## Concluídos
