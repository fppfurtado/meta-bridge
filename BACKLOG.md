# Backlog

## Próximos

- **`/journal-review` heurística `bucket-co-occurrence`**: detecta buckets A+B que coocorrem em ≥ N journals da janela e propõe fusão A∪B (operador define nome alvo). Adiada pra v2 do `/journal-review` em 2026-06-12 via `/triage` (sessão `refinamento-journal-close` → expansão `/journal-review`). Razão da postergação: heurística estrutural com baixa frequência esperada num plugin de uso pessoal; threshold N ainda não calibrado contra dado real. **Contexto técnico**: análise por co-occurrence dentro de janela `--days N`; threshold N proposto = 5 mas precisa calibração empírica. Risco se materializar prematuro: falsos positivos em buckets cross-cutting genuínos (epics multi-domínio). **Gatilho de execução**: N ≥ 2 reports manuais do operador de buckets coocorrentes que mereceriam fusão sem que `/journal-review` v0.3.0 (MVP) tenha proposto. Cross-refs: `.claude/local/plans/journal-review-refactor.md` § Resumo da mudança; ADR-001 § Sub-decisão 10.

- **`/journal-review` heurística `bucket-rename-implicit`**: detecta bucket A que para de aparecer em data D e bucket A' que começa a aparecer com conteúdo análogo, propõe confirmar rename e propagation de tasks órfãs. Adiada pra v2 em 2026-06-12 via `/triage`. Razão da postergação: detecção semântica difícil ("conteúdo análogo" exige similarity score sobre prosa livre); risco alto de falso-positivo em hashtags de tópico similar mas semanticamente distintos (ex.: `#meta-portability-mcp` vs `#meta-portability` foram rename real, mas `#meta-bridge` vs `#meta-system` são domínios distintos). **Contexto técnico**: requer comparar buckets antes/depois de gap de N dias; signal "A→A'" precisa critério forte (≥ K tasks/narrativas comuns); operações em cascata (renomeação de bucket em journals históricos é mass modify cross-file de alto impacto). **Gatilho de execução**: N ≥ 2 incidentes reportados pelo operador de bucket sumir/renomear sem propagation manual + N ≥ 1 ocorrência de tasks ficarem órfãs por rename não-propagado. Cross-refs: `.claude/local/plans/journal-review-refactor.md` § Resumo da mudança; ADR-001 § Sub-decisão 10.

- **`/journal-review` heurística `bucket-naming-drift`**: detecta variantes superficiais do mesmo conceito (`#meta-bridge` vs `#metabridge` vs `#meta_bridge`) e propõe normalizar pra forma canonical. Adiada pra v2 em 2026-06-12 via `/triage`. Razão da postergação: drift de naming em uso pessoal é raro (operador é o único produtor de buckets); criação de hashtags passou por `sanitização kebab-case lowercase` per ADR-002 Sub-decisão 3 (meta-system) desde Onda 4.5 — o canal de drift está fechado por construção exceto edição manual no Logseq desktop. **Contexto técnico**: detecção por Levenshtein distance ≤ 2 entre nomes de bucket (com filtro de buckets reconhecidamente distintos `#meta-system` vs `#meta-bridge`). **Gatilho de execução**: N ≥ 2 reports manuais do operador de variantes do mesmo bucket coexistindo + drift originado por edição manual no Logseq desktop (não por skill do plugin). Cross-refs: `.claude/local/plans/journal-review-refactor.md` § Resumo da mudança; ADR-001 § Sub-decisão 10; ADR-002 Sub-decisão 3 do meta-system (sanitização canonical).

- **`/journal-review` apply estrutural automático (heurísticas 3-4 `bucket-underused` + `bucket-emerging`)**: MVP v0.3.0 emite findings report-only com evidência detalhada; operador aplica mass modify cross-files manualmente via Edit/Write. Apply automático com snapshot defensivo em path canonical fora do graph (XDG cache padrão) reabre em v0.4.0 sob gatilho. **Contexto técnico**: snapshot per-finding-type protege journals históricos pré-write; mass modify cross-files (mover bucket pra page agregadora, criar bucket emergente + migrar referências) é alto risco de drift semântico — preview-first inline mitiga matching errado, mas erro detectivo apply'd cascateia. **Gatilho de execução**: N ≥ 2 reports manuais do operador de findings report-only das heurísticas 3-4 que mereceriam apply automático — sinal real de que dor de aplicar manualmente compensa complexidade snapshot per-finding-type + interaction matrix mais complexa com `/journal-close`. Cross-refs: ADR-001 § Sub-decisão 10.

- **Revisitar calibração K/M de `/journal-review` heurística 2c**: gatilho condicional emerge quando (a) graph atingir densidade dobrada (≥ 30 journals na janela default 30d) com > 4 findings `bucket-underused` simultâneos, OU (b) refactor semântico AND→OR da heurística for sobre a mesa. Calibração atual K=M=2 é o piso útil pra densidade corrente (~17 journals, 4 findings em 2026-06-15). Cross-refs: ADR-001 § Sub-decisão 10 Adendo v0.3.2 § Descoberta editorial sobre 2c (registro aritmético + safeguard).

## Concluídos

- 2026-06-18 — `/journal-load` Step 5 drift (echo verbatim ≠ load em working memory): Step 5 renomeado "Reportar sumário"; dump verbatim removido; skill emite apenas sumário curto de contagem + janela. Adendo (2026-06-18) a ADR-001 Sub-decisão 9 codificando invariante "load ≠ surface; primitiva ≠ echo"; Step 4 da SD9 atualizado removendo template verbatim obsoleto. 3 findings prompt-reviewer absorvidos (formato data, explicitação "matches", restrição inline no Step 5); 1 finding doc-reviewer cutucado — operador escolheu "remover texto contradito" (Step 4 SD9). Commits `5fd3c2e` (SKILL.md) + `f9a755e` (ADR-001 SD9).

- 2026-06-18 — Hook `session-start-tip`: fix path-vs-Repo basename mismatch. Refactor de `_load_owned_active` retornando `dict[str, str]` (match_key → bucket_name) em vez de `set[str]`; novo helper `_derive_basename` encapsula strip 3-pattern (`.strip().strip("`").strip()`) + `os.path.expanduser` + `os.path.basename`. Cada entry owned/active gera chave canonical (Repo field) SEMPRE e chave derivada (basename do Path expandido) CONDICIONALMENTE quando diverge. main() passa a usar `dict.get(basename)`; tip cita o bucket name canonical independente da chave que casou. Suite pytest expandida pra 19 testes (12 existentes atualizados de set→dict + 7 novos: derivação Path quando diverge, expanduser, Path vazio, casing match, colisão last-write-wins, helper isolado, E2E `main()` via fake git repo). 6 cenários manuais PASS in vivo (Cenário 1 regression canonical em meta-bridge, Cenários 2-4 path basename matching em logseq-notes/dotfiles/scripts — os 3 repos que silenciavam agora emitem tip canonical, Cenário 5 no-match silent em `/tmp`, Cenário 6 filtro NEGATIVO subsection em splitrail silenciado independente da chave) + Cenário 7 suite pytest 19/19 verde — 7/7 cenários totais validados. Decisões F3/F4/F5/F6 design-reviewer absorvidas pré-commit no `/triage` (strip pattern 3-strip + collision regression + cenário 6 manual delegado a pytest + helper isolado). Cross-ref: bug detectado em `/run-plan §3.2` cenário 8 cross-cluster sanity (2026-06-16) — gatilho N≥1 disparado e endereçado aqui. Commits `8006dc9` (hook refactor) + `715e0e4` (suite expansion); release pendente em sessão dedicada pós-merge.

- 2026-06-18 — Pytest formal pro sub-tool `skills/wiki-compile/sub-tools/compile.py`. Suite com 13 testes cobrindo 5 cenários do plano + 3 gaps absorvidos do qa-reviewer (E2E ordem canonical via `main()`; shape de prefix de `append_to_section`; 3 testes `validate()` fail-fast em input inválido). Pattern isomorfo ao precedente `tests/test_suggest_session_start_tip.py` (importlib.util + monkeypatch sys.argv; sub-tool sem side-effects no module load — verificado pelo design-reviewer pré-execução). Materializa pré-condição "eficiência" do gatilho intermediário ADR-021 § auto-crítica permanente (meta-system) antes da Onda 3 do roadmap knowledge layer block-first. Regression do bug histórico `find_insert_position` ignorando anteriores (descoberto em `/run-plan onda-2-knowledge-layer-piloto` do meta-system 2026-06-18, 6ª chamada inseriu Sources digeridas ANTES de Notas curadas violando ordem canonical) cristalizado explicitamente no shape (c) do Cenário 1. Decisões F1/F2/F3 design-reviewer absorvidas pré-commit no `/triage`; gaps Q1/Q2/Q3 qa-reviewer absorvidos pré-commit no bloco 1 via cutucada de escopo (operador escolheu expansão). Cross-repo: satisfaz pendência registrada em `meta-system/docs/plans/onda-2-knowledge-layer-piloto.md` § Pendências de validação. Commit `94f570e`; release pendente em sessão dedicada pós-merge.

- 2026-06-16 — Hook bridging SessionStart (`hooks/suggest_session_start_tip.py`). 2ª trajetória de hook bridging cwd-aware sugerindo `/journal-load --days 2 --bucket <repo>` quando cwd resolve via `git rev-parse --show-toplevel | xargs basename` para basename owned/active de `~/Projects/meta-system/REPOS.md` (parser markdown com filtro NEGATIVO — exclui overview `## Clusters (N)` + subsection `### Runtime auxiliar consumido externo`). Hook script Python standalone isomorfo a `suggest_journal_close.py` (Sub-decisão 6 v0.1.5) + binding SessionStart em `hooks/hooks.json` + Adendo v0.2.0 em ADR-001 Sub-decisão 6 (registro factual; sem generalização prematura per F1 design-reviewer) + Adendo a ADR-002 § Decisão 6 refinando critério "sem suite formal" → "suite parcial sob parsing-complexo" + suite pytest inaugural `tests/test_suggest_session_start_tip.py` (12 testes — 6 unit em `_load_owned_active` + 6 e2e in-process em `main()`). Decisão F2 design-reviewer inverteu pré-decisão upstream original (CLI sub-comando `mb session-start-tip` → hook standalone) eliminando item (d) cross-repo chezmoi inteiramente — wiring automático via `hooks.json` no plugin install. Decisões F3 (parser filtro NEGATIVO) + F4 (Adendo ADR-002 reconciling drift doutrinal) + F1/F5 absorvidos pré-commit. Cenário 8 validação manual revelou finding capturado no BACKLOG (path-vs-Repo basename mismatch em logseq-notes/dotfiles/scripts). Commits `a9e8304` (hook script + binding), `4a52a2f` (suite pytest), `691d9a6` (Adendo ADR-001 v0.2.0), `e4dec78` (Adendo ADR-002 Decisão 6), `0fe6a64` (realign CLAUDE.md + README + manifests); release pendente em sessão dedicada pós-merge.

- 2026-06-16 — Housekeeping `## Status` em 3 planos locais concluídos. `.claude/local/plans/journal-review-refactor.md`, `materializar-cli-mb.md` e `calibracao-thresholds-journal-review.md` tinham `## Status: Pendente` no body mas estavam factualmente concluídos (PRs #1 `a6cda27`, #2 `d3b7b18`, #3 `eb621b8` mergeados). Edit cirúrgico `Pendente`→`Concluído` em cada body via `/triage`. BACKLOG entry original (detectada em `/next` 2026-06-15 step 4.6) cobria 2 planos; 3º (`calibracao-thresholds`) absorvido como side-effect coerente do mesmo drift type — pattern "drift retroativo corrigido como side-effect coerente" já estabelecido no plugin (journal 2026-06-13 sobre `/journal-load` no README durante rename `/weekly-review`→`/journal-review`).

- 2026-06-15 — `/journal-review` recalibração T/N + flags opcionais semânticos + descoberta editorial sobre 2c. Defaults T=14→21 (heurística 2b `task-zombie`) e N=3→4 (heurística 2d `bucket-emerging`) ajustam ambas heurísticas pra reduzir falsos positivos no graph pessoal. **K e M de 2c preservados em 2** após validação manual contra inventory real revelar erro aritmético na evidência original do BACKLOG entry: "5/6 buckets disparariam com K=2/M=2" foi inferido ignorando a conjunção `<K AND <M` da heurística — no inventory real só 4 buckets disparam (chezmoi, logseq-notes, meta-portability-mcp, youtube-context); subir K/M *aumentaria* o conjunto "underused" (conjunção alarga com thresholds maiores), e baixá-los seria degenerado (K'<1 impossível). 4 flags semânticos opcionais shipados (`--bucket-min-journals`, `--bucket-min-tasks`, `--zombie-days`, `--emerging-min-mentions`) permitem override caso-a-caso. Adendo v0.3.2 em ADR-001 Sub-decisão 10 documenta a descoberta editorial e marca gatilho de revisão futura pra 2c (densidade dobrada ou refactor AND→OR). README.md `## What's inside` atualizado com os 4 flags + defaults K=2/M=2/T=21/N=4. Commits `ad2f460` (Bloco 1 SKILL.md original com K=3/M=3 + 4 flags), `4cc1413` (Bloco 2 Adendo v0.3.2 original), `c72cb2c` (README sanity-check), `227d34e` (fix consolidado preservando K=M=2 + Adendo reescrito + README ajustado pós-validação).

- 2026-06-15 — Materialização CLI `mb` substituindo Tier 1 MCP candidato implícito (per `meta-system` ADR-016 critério target-aware aplicado: MCP-1/2/3 = 0/3 vs CLI-1/2/3/4 = 4/4). Pacote Python `meta_bridge` (Click + hatchling) com entry-point `mb` instalável via `pipx`; 4 subcomandos (`journal-note`, `journal-close`, `journal-review`, `init-project`); `/journal-load` permanece MD-only. 4 SKILL.md viram thin orchestrators preservando substância heurístico-semântica (matching, princípios editoriais, 4 heurísticas, cluster prompt). ADR-002 novo + Adendos cirúrgicos em ADR-001 Sub-decisões 1, 3, 4, 10. Decisões F1/F2/F3/F4/F5/F6/F7/F8/F9 absorvidas via design-reviewer + code-reviewer + prompt-reviewer + doc-reviewer durante `/run-plan materializar-cli-mb` em worktree isolada. Faceta (c) cross-repo (snapshot `TIER_SNAPSHOT_2026_06_11` em `meta-portability-mcp`) out-of-scope — coordenada via plano runbook em `meta-system`. Commits `59a4d7b` (skeleton), `e7ece02` (gitignore uv.lock), `5d83d34` (journal-note), `779202c` (NFD sanitization fix), `5a0afdf` (journal-close), `196f46d` (journal-review), `647ed73` (init-project), `97ddc12` (skills thin orchestrators), `93db0a0` (ADR-002 + Adendos), `8238e76` (realign editorial); release pendente em sessão dedicada pós-merge.

- 2026-06-13 — Fix drift Sub-decisão 10 ADR-001: incluir DOING em H1 (`task-closure-by-context`) e H2 (`task-zombie`) na prosa descritiva dos bullets (linhas 558/559). SKILL.md `/journal-review` já consistente em H1/H2/Step 7 desde release v0.3.0 — drift era puramente textual no ADR. Edição direta inline (sem Adendo — SKILL nunca esteve fora de sync; tratado como typo doutrinal silente). Step 7 (linha 567) e Step 3a regex (linha 551) já corretos no original — escopo real menor que o reportado na entry.

- 2026-06-13 — Refactor `/weekly-review` v0.2.0 → `/journal-review` v0.3.0 (detective-first com heurísticas estruturais + janela configurável + wizard opt-in). 4 heurísticas MVP (`task-closure-by-context` + `task-zombie` apply; `bucket-underused` + `bucket-emerging` report-only); janela `--days N` default 30 inclusiva `[hoje-N, hoje]` ou `--from/--to` opcional; `--interactive` opt-in pra wizard residual; `--write-summary` opt-in pra bloco no journal de hoje. ADR-001 Sub-decisão 10 nova com cross-ref retroativo a Sub-decisão 5 histórica; rename de skill; drift retroativo `/journal-load` na tabela do README corrigido como side-effect coerente. Commits `aaee0b1` (ADR), `d09fd18` (SKILL rename + reescrita), `dc95e27` (CLAUDE realign), `984b7f3` (descriptions JSONs), `ad36d94` (README realign + drift retroativo); release pendente em sessão dedicada pós-merge.

- 2026-06-12 — Refactor editorial `/journal-close` v0.4.1 (3 refinamentos nos princípios de síntese: granularidade DONE por conceito não commit; filtros de seleção em "Material editorial"; 3 antipadrões em "O que NÃO fazer"; ADR-001 Sub-decisão 3 § Adendo v0.4.1). Commit `894e930`; release [v0.5.1](https://github.com/fppfurtado/meta-bridge/releases/tag/v0.5.1).

- 2026-06-12 — Refactor `/journal-close` v0.4.0 (reconciliação prévia com janela `--days N` + modify-in-place no source pra TODO/WAITING fechados pela sessão; ADR-001 Sub-decisão 3 § Adendo v0.4.0). Commits `cc7f225`, `a693902`; release [v0.5.0](https://github.com/fppfurtado/meta-bridge/releases/tag/v0.5.0).

- 2026-06-11 — Refactor `/journal-close` v0.3.0 (escopo expandido DONE + TODO/WAITING + insights/reflexão direcional; template humano-amigável 2ª pessoa substituindo schema git-log-like flat; ADR-001 Sub-decisão 3 § Adendo v0.3.0). Commits `32ad30c`, `9365efb`; release [v0.3.0](https://github.com/fppfurtado/meta-bridge/releases/tag/v0.3.0).
